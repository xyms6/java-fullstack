# Configuração do PostgreSQL no Render
spring.datasource.url=jdbc:mysql://cadastro-usuario-srtoddy-0b33.e.aivencloud.com:3306/cadastro-usuarios?sslMode=REQUIRED
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_lyMLQ_Rpu4BR1AA7Idk


# Configuração do JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Configuração do servidor
server.port=${PORT:8080}

# Desativa telemetria se não estiver usando
management.otlp.metrics.export.enabled=false
management.tracing.enabled=false
